// Shared game-related constants usable by both Convex server functions and client code.
// Keep values primitive (no environment or platform assumptions) so they stay isomorphic.

export const WAITING_GAME_TTL_MS = 5 * 60 * 1000; // 5 minutes
